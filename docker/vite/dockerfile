FROM node:22

WORKDIR /app

# 依存インストール（キャッシュ活用）
COPY ./frontend/package*.json ./
RUN npm install

# ソースコードコピー
COPY ./frontend ./

# --- 本番ビルド ---
RUN npm run build

# dist を表示（デバッグ用）
RUN ls -l /app/dist
